# Seed and Import Templates

These templates define accepted import columns. They do not contain the full real KERC staff list because the source requires human verification. Use the application's mapping/review workflow to import the source document after conversion to CSV/XLSX.

No import creates login accounts automatically.
