# External allocation boundary

External-driver allocation is intentionally separate from employee-driver allocation. The final assignment keeps `vehicle_allocations.driver_employee_id` empty and records the verified external identity, licence snapshot and acceptance evidence in `external_driver_assignments`.
