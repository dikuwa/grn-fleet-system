# External allocation workspace

Transport Administration uses this workspace only for approved external requests and verified external drivers. Assignment remains pending until Transport records the external driver's communicated acceptance or cancellation reason.
